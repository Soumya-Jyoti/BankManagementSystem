create database bankSystem;
use bankSystem;
create table signup(from_no varchar(10), name varchar(30), father_name varchar(30), DOB varchar(20), gender varchar(30), email varchar(60), marital_status varchar(30), address varchar(60), city varchar(30), pin varchar(10), state varchar(30));
select * from signup;